# 验收

*English (primary): [ACCEPTANCE.md](ACCEPTANCE.md)*

**这份文件是整套制度里最贵的一块。** 其余规矩都在为它服务。

---

## 什么算实物

| 算 | 不算 |
|---|---|
| 改动本身(diff) | "我改完了" |
| 磁盘上的文件、目录状态 | "文件已生成" |
| 检查/构建/测试的**输出** | "测试都过了" |
| 能打开的页面、能看见的界面 | "页面正常" |
| 版本控制里的真实状态(分支、提交、干净的工作区) | "已提交" |

**判据:第三方能不能自己把它调出来看一眼。** 不能,就不是实物。

---

## 三值一致

验收一个改动,先对三个值:**分支 / 提交 / 变更内容**。

汇报里说的、版本控制里的、你自己看到的 —— 三者对不上,先停下来问,不要继续往下验。

---

## ⭐ 刀验(mutation check)

**这是本文的核心。**

一个测试通过,只能证明"当前代码没让它红"。它证明不了**这个测试真的在守着什么**。

所以:**把被保护的行为故意改坏,看该测试会不会红。**

```
不红 = 那颗钉子是假的。
```

### 怎么做

1. 在**已提交的**代码上原地施加改动(不是在未提交的工作区上,否则容易把别人的东西吃掉)。
2. 跑**全量**检查,不是只跑那一个文件 —— 你要知道**红了几条、红在哪**。
3. 记下**确切的失败信息**。"红了"不算记录,失败信息才是。
4. `checkout` 还原,并用"差异为空"自证还原干净。

### 刀要打在哪

**打在"最像正确、其实错了"的地方**,不要打在明显错的地方。

| 差的刀 | 好的刀 |
|---|---|
| 把整个函数删掉 | 把两个动作的**顺序**对调 |
| 把常量改成明显荒谬的值 | 把常量改成**另一个合理的值** |
| 删掉整段配置 | 把事件名换成**另一个合法但不对的名字** |
| 让代码报错 | 让代码**照常跑通,只是行为悄悄变了** |

> **一个真实的教训:**同一个漏洞,把干扰项**插在前面**会红,**追加在后面**却全绿 —— 因为断言只检查了"存在",没检查"只有一个"。**刀的形状不同,结论就不同。**

### 谁来打

**审查方打一遍,枢纽自己再打一遍,而且要打不同的刀。**

审查方通常验"东西在不在";枢纽应当补上"东西在、但是错的"那一类。**两边打同样的刀等于只打了一遍。**

---

## 走快道还是慢道

- **快道**(枢纽自己验完即可):纯文档、改名、删死代码 —— 以及**能量出"零行为改动"**的改动。
- **慢道**(必须过独立审查):任何改变行为的东西。
- **拿不准走慢道。**

> "零行为改动"要**量出来**,不是感觉出来:把非产品文件过滤掉之后,差异是不是空的。

---

## 报"可以合并"之前的清单

1. 三值一致
2. 自动检查**全绿**(不是"应该会绿")
3. 独立审查的结论已到,或已明确判定走快道**并写下依据**
4. 刀验记录齐全,含确切失败信息
5. 是不是堆叠改动?合并后要不要删分支?**有堆叠就提醒先别删**
6. 合并之后需要人做什么(重新同步配置、跑一次上线动作、打一通验证电话……)——**在报告里一次说清**

---

## 一条给枢纽的自律

**下级写下的"理由",在你转述之前,自己先核一遍。**

判据可以是对的,理由可以是假的。人是听着那个理由做决定的 —— **你转述了假理由,人是在假前提下批准的,即使结论恰好正确。**
