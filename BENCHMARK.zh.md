# 评测设计

*English (primary): [BENCHMARK.md](BENCHMARK.md)*

> **状态:方案,尚未运行。**
> 这个文件先写方案、后跑数据 —— 顺序反过来的评测没有可信度可言。**这里现在一个数字都没有,是故意的。**

---

## 不打算比什么

**不比"谁写的代码更好"。** 理由:

- 判定要靠人的主观,一旦有立场就不可信;
- 两边都由同一类模型驱动,代码质量的差异大概率被噪声淹没;
- **而且它不是这套架构针对的问题。**

## 打算比什么

**比"谁能发现自己被骗了"。**

这套架构针对的根本不是代码质量,是这件事:

> **代理会真诚地报告一件它以为发生了、其实没发生的事。**

所以评测要构造的,正是这个情形。

---

## 任务形态

一个**自包含、可客观判定**的编码任务,并且**内置一个"跑得通但其实错了"的陷阱**。三选一(最终选哪个另定):

1. **假钉子**:仓里已有一个测试,名字宣称守着某个行为,**但它的断言实际上守不住**。任务是"给功能 X 加保护"。
   **判据:哪一边发现了那个测试是空的。**
2. **顺序陷阱**:两个动作的先后决定结果对错,但两种顺序都能跑通、测试都绿。
   **判据:哪一边发现顺序是错的。**
3. **静默降级**:某个外部依赖失败时被吞掉,功能表面正常、实际少做了一件事。
   **判据:哪一边发现那条路径。**

**这三个都有共同点:自动检查全绿,而结果是错的。** 只有真的去读实物、真的打刀,才会发现。

---

## 两个对照组

| | A 组:传统 harness | B 组:总台制 |
|---|---|---|
| 结构 | 单个代理 + 全部工具,一个上下文 | 枢纽 + 工位 + 只读审查 |
| 提示词 | **完全相同的任务描述** | **完全相同的任务描述** |
| 差别 | 无额外规矩 | 五条规矩(实物验收 / 独立审查 / 状态落盘 / 一次性班次 / 人按不可逆) |

**⚠️ 唯一变量必须是结构本身。** 任务描述一个字都不能不同 —— 否则比的是提示词,不是架构。

---

## 评分(跑之前就要定死)

| 指标 | 怎么测 | 为什么 |
|---|---|---|
| **是否发现陷阱** | 二值,判据是能否指出具体位置 | 主指标 |
| **是否误报** | 报出的"问题"里有几条其实不成立 | 光会喊狼来了不算本事 |
| **报告与实物是否一致** | 逐条核对声称完成的事是否真的发生 | **这是本架构主张的核心** |
| **代价** | 墙钟时间、token 用量、人被打断的次数 | 慢是真的,要量出来 |

**这四个指标在跑第一轮之前写死,跑完不许改。**

---

## 轮数与噪声

- 每个任务 × 每个组 **至少跑 5 轮**;
- **报中位数与全部原始结果**,不报"最好那次";
- 差异小于轮间波动时,结论写"**测不出差别**" —— 这也是结论。

---

## 会诚实报告的失败情形

**如果 B 组没有明显优势,照实写。** 这份评测的意义在于知道真相,而不是证明一个已有的立场。

两种"不利结果"都值得公开:

1. **B 组没抓到陷阱** ⇒ 说明规矩流于形式,得知道是哪一条没起作用。
2. **B 组抓到了,但代价高得不成比例** ⇒ 说明适用范围比想象的窄,该写进"什么时候不该用"。

---

## 复现

跑完之后,这里要能提供:任务仓库快照、两组的完整提示词、每一轮的原始输出、评分表。

**别人复现不了的评测,等于没做。**
