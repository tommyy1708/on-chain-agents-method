# 协议

*English (primary): [PROTOCOL.md](PROTOCOL.md)*

这份文件是**给每个工位读的**。把它(或它的链接)放进每个工位的说明文件里,新班次开工时会自动读到。

---

## 角色

| 角色 | 职责 | 硬边界 |
|---|---|---|
| **人** | 拍板;按下所有不可逆的动作 | 只与枢纽对话 |
| **枢纽** | 写工单、派活、验收、记账、向人汇报 | **不写产品代码**(验证用的临时改动除外,且必须还原到字节一致) |
| **工位** | 干活,产出实物 | 只碰自己的属地;不合并、不上线 |
| **审查** | 读实物、给结论 | **零写权限;不与工位直接通信** |

**拓扑是枢纽制**:外围工位之间**互不直连**。审查与作者尤其不直连 —— 这是审查独立性的物理保障,不是礼貌问题。

---

## 信箱

```
mailbox/
  to-hub/          工位 → 枢纽(回信、数据、申请)
  to-<station>/    枢纽 → 某个工位(工单)
  archive/         处理完的消息全部移到这里
```

**一个话题一个文件**,命名 `YYYY-MM-DD-<slug>.md`。

### 消息头

```markdown
---
status: NEW            # NEW / ACKED
from: hub              # hub / <station 名>
needs: <这封信要对方做什么>
reply: to-hub/YYYY-MM-DD-<slug>.md
---
```

### 状态机

`NEW` → 收件方处理 → **在文末追加处理结果** → 改成 `ACKED` → 移入 `archive/`。

**追加处理结果这一步不许省。** 归档时留下的痕迹,是三个月后唯一能回答"这件事当时怎么定的"的东西。

### 谁能写谁

- 工位**只写** `to-hub/`,**只读**发给自己的那个目录。
- **不许**工位之间直接投信。需要另一个工位配合的,写给枢纽,由枢纽转写。
- 账本**只有枢纽写**。工位可以读,绝不改。

---

## 工单

用 `templates/work-order.md`。一份合格的工单必须回答四件事:

1. **要什么** —— 目标,以及怎样算完成。
2. **怎么做** —— 执行方案:顺序、用什么工具、失败了怎么办。**「怎么做」的判断是派单方的活,不许外包给执行方。**
3. **不许碰什么** —— 硬边界。越界即返工。
4. **怎么证明** —— 交付时要附什么实物。

> **一条经验:**边界写成测试,比写在工单里牢。工单会过期,钉子不会。

---

## 班次

每次开工 = 一个**全新的一次性进程**,在该工位自己的目录下运行,只带这一单的工单。

- **无记忆**:不知道上一班干了什么,靠工单 + 磁盘上的实物工作。
- **窄权限**:工具允许清单按这一单的需要给。只读的活,就别给写权限。
- **一个调用派一个班次**,不要在一条命令里串两个 —— 一起被杀时两个都没有产出。
- **后台跑**,让驾驶座保持空闲。跨工位可以并行,同工位排队。

### 提示词里必须写清的三件事

1. 工单路径,以及"严格照办"。
2. 本班次的硬护栏摘要(不部署、不碰生产、不打真实外部调用、开 PR 不合并……)。
3. 回信路径与格式。

---

## 账本

一个 JSON 文件,**只有枢纽写**,记三样:

- 每个工位现在**是空闲还是在跑**、在跑什么、从什么时候开始;
- **等人拍板**的事项;
- **留痕**:每一次派单、验收、决定,连同**当时的理由**。

**派活/验收和记账必须是同一个动作完成。** 漏记一次,状态显示就在撒谎。

> 结构见 `ledger.example.json`;`scripts/statusline.sh` 读它,把全员状态显示在命令行底部。

---

## 汇报给人

**结构固定,顺序不许改** —— 顺序本身是为了让人读前两行就能决定要不要往下读。

```
需要你:<不用动 | 拍板 | 合并 | 去某处操作>
结论  :<一句话>

问题  :<这件事到底是什么>
为什么:<不做会怎样 —— 用后果说话,别用原理说话>
怎么核的:<自己跑的?独立验证过?还是只看了汇报?—— 他据此决定信多少>
卡着谁:<等人 / 等某工位 / 谁都不等>
```

**默认三到五行封顶**,细节写进文件,对话里最多给一句指路。**「怎么核的」不许省** —— 自己跑的、独立验证的、只看汇报的,可信度完全不同。

**一次只讲一个任务。** 三件事就三块,中间分开。
