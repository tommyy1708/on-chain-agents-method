---
status: NEW
from: review
re: to-review/YYYY-MM-DD-<slug>.md
blockers: B1, B2
---

# Verdict: **2 个 blocker**

<第一行就给结论:**可以合**,或 **N 个 blocker**。标题说的是同一件事 —— 只读到标题就停下的人,
不该得到另一个答案。>

## Blocker

<每条一个 `### Bn · ` 标题,从 B1 起编号,连续,不许跳号。回信里别的东西一律不给 B 编号。
**`blockers:` 这一行最后填** —— 先填清单,等于在搜之前就报了数。>

### B1 · <一行标题>

**位置**:`path/to/file:line`
**证据**:<你跑了什么、它打出了什么。没有实物的断言不是 blocker,是观点。>
**建议改法**:<怎样才算对。作者可以选别的写法;所以要说清**要保住的是什么**,而不只是怎么拼。>

### B2 · <一行标题>

**位置**:`path/to/file:line`
**证据**:<原样贴>
**建议改法**:<…>

## Should-fix

<该改,但不拦合并。**这一节不许出现 B 编号** —— 编号意味着合并要停下,别的东西不许占。>

- <结论,带位置>

## Not verified(没能核的)

<你没能核的东西:没跑的命令、没 checkout 的分支、只靠读代码推断出来的结论。

**要写清用的是什么方法,而不只是结论。**「合并冲突我实测不了,这是推断不是测量」这种话,
比任何评语都有用 —— 枢纽可以据此自己去测一遍。

**不许用推理冒充测量。** 如果这一节是空的,把「跑了什么才让它空」写出来。>

## Guardrail self-check(护栏自证)

<逐条对着审查工单的禁止项回答,附收尾时的实际状态。

- 刀在哪儿打的(临时目录),以及原件字节一致。
- 没写、没提交、没推送、没在被审对象上留评论。
- 没和工位接触,结论只回给枢纽。>

---

> **这份回信故意没有「我发现但没做的事」这一节。** 那一节是为了接住工位没能扛下来的活。
> **审查不动手,它没有「没做的活」** —— 对应的诚实写法是上面的「Not verified」。
>
> 闸门(`scripts/check-transcription.sh`)只认这两种写法的字面形式:
> - `blockers: B1, B2` —— 分隔符可用 `,` `，` 或 `、`;一个都没有就写 `blockers: none`,
>   并且**正文里不许再有任何 `### B` 标题**。
> - `### B1 · <标题>` —— `###`、空格、`Bn`、空格、`·`(U+00B7)、空格、标题。
>   标题的集合必须与 `blockers:` 清单**完全一致**。
>
> 以 `### B` 开头的行永远是真 blocker。要举例说明格式,就放进代码块。发出去之前自己核一次:
> `scripts/check-transcription.sh <这份回信> <一份返工单草稿>`。
